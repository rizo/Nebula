<src>: include
<src/devices/*.cmx>: for-pack(Devices)

<lib>: include
<lib/functional/*.cmx>: for-pack(Functional)

true: safe_string, bin_annot, thread, warn_error(+8)

true: package(cmdliner), \
      package(ctypes), \
      package(ctypes.foreign), \
      package(ppx_deriving.std), \
      package(tsdl)

<test/*>: debug, package(oUnit)

<top/*>: package(utop)
