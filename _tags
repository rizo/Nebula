true: safe_string, bin_annot, thread, warn_error(+8)

<src>: include
<src/Devices>: include
<src/Devices/*.cmx>: for-pack(Devices)

<src/**> or <top/**> or <test/**>: \
      package(cmdliner), \
      package(ctypes), \
      package(ctypes.foreign), \
      package(ppx_deriving.std), \
      package(tsdl)

<top/**>: package(utop)

<test/**>: package(oUnit)

<lib/functional>: traverse, include
<lib/properties>: traverse, include
