true: safe_string, bin_annot, thread, warn_error(+8), package(unix)

<src>: include, traverse

<src/**> or <top/**> or <test/**>: \
      package(cmdliner), \
      package(ctypes), \
      package(ctypes.foreign), \
      package(ppx_deriving.std), \
      package(tsdl)

<top/**>: package(utop)

<test/**>: package(oUnit)

<lib/functional>: include, traverse
<lib/properties>: include, traverse
