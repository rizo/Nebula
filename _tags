true: safe_string, bin_annot, thread, warn_error(+8), \
      package(ctypes), \
      package(ctypes.foreign), \
      package(result)

# Emulator

<src/emulator>: include, traverse

<src/emulator/**> or <top/emulator/**> or <test/emulator/**>: \
      package(cmdliner), \
      package(ppx_deriving.std), \
      package(tsdl)

<top/emulator/**>: package(utop)

<test/emulator/**>: package(oUnit)

# Assembler

<src/asm>: include, traverse

<top/asm/**>: package(utop)

# Libraries

<lib/functional>: include, traverse

<lib/properties>: include, traverse

<lib/word>: include, traverse
